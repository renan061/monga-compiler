/* AST */

/* Global variables */
int intvalue;
float floatvalue;
char charvalue;

/* ParamNode */
void x() {}
void y(int a) {}
void z(int a, float b, char c) {}

void foo(char a, int b, float c) {
/* CmdBlock without vars and cmds */
}

void bar(int intvalue1, int intvalue2) {
/* CmdBlock with vars and without cmds */
	/* TypeNode */
	int i;
	float f;
	char c;
	int[] x;
	int[][] y;
	int[][][] z;
	/* Var definition with list of names */
	int a, b;
	float a, b, c;
	char a, b, c, d, e;
}

void baz(int intvalue1) {
/* CmdBlock without vars and with cmds */
	a = 1;
	b = 2;
	c = 3;
}

void main(char[] a, int[][] b, float[][][] c) {
/* CmdBlock with vars and cmds */
	int a;
	float b;
	char c;

	/* Cmds */
		/* CmdIf */
		if (1 || 2) {
			a = 3;
		}
		/* CmdIfElse */
		if (1 || 2) {
			a = 3;
		} else {
			a = 4;
		}
		/* CmdWhile */
			while(1 || 2) {}
			while(1 || 2) {
				a = 1;
				b = 2;
				c = 3;
			}
		/* CmdAsg */
			a[1] = 2 || 3;
		/* CmdReturn */
			return;
			return 1 || 2;
		/* CmdCall */
			foo();
			foo(1);
			foo(1, 2, 3);
		/* Crazy cmds */
		if (1)
			if (2)
				a = 3;
			else
				a = 4;
		else if (5)
			if (6)
				a = 7;
			else
				while(8)
					if (9)
						a = 10;
					else if (11)
						a = 12;
					else 
						a = 13;

	/* Vars */
		/* VarId */
		a = b;
		/* VarIndexed */
		a = b[1];
		a = b[1][2][3];
		a = b[0 >= 5][foo(1, bar(0 && 1))][1 + a[3]];

	/* Exps */
		/* ExpKInt */
		a = 1;
		/* ExpKFloat */
		a = 1.1;
		/* ExpKStr */
		a = "111";
		/* ExpVar */
		a = b;
		/* ExpCall */
		a = foo();
		a = foo(1);
		a = foo(1, 2);
		a = foo(1, 2, 3);
		/* ExpNew */
		a = new int[2];
		/* ExpUnary */
		a = -1;
		a = !1;
		/* ExpMul */
		a = 1 * 2;
		a = 1 / 2;
		/* ExpAdd */
		a = 1 + 2;
		a = 1 - 2;
		/* ExpComp */
		a = 1 == 2;
		a = 1 <= 2;
		a = 1 >= 2;
		a = 1 < 2;
		a = 1 > 2;
		/* ExpAnd */
		a = 1 && 2;
		/* ExpOr */
		a = 1 || 2;
		/* Crazy Exps */
		a = -1 || 2 * 3 >= 4 + 5 / 6;
		a = (0 && 1) + foo(2, bar(1 + 3 / 7)) / a * 3 + (new int[7.5]);
}